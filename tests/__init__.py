"""Test package for OmniAgent Flow.

Modules here are designed to run with pytest, but we still expose a
``__main__`` block for the legacy ``python test_xxx.py`` entry points
that already exist in the repo, so the project's smoke scripts keep
working.
"""
